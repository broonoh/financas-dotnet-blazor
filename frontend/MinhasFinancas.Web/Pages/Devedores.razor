@page "/devedores"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
@using MinhasFinancas.Web.Services
@using MinhasFinancas.Web.Models
@inject DashboardApiService ApiService

<MudText Typo="Typo.h4" Class="mb-1">Devedores</MudText>
<MudText Typo="Typo.body2" Color="Color.Secondary" Class="mb-4">
    Cadastre as pessoas ou empresas que devem valores a você. Elas ficarão disponíveis para seleção em Contas a Receber.
</MudText>

<MudPaper Elevation="2" Class="pa-4 mb-4">
    <MudText Typo="Typo.h6" Class="mb-3">@(_editandoId == null ? "Novo Devedor" : "Editar Devedor")</MudText>
    <MudGrid>
        <MudItem xs="12" md="6">
            <MudTextField @bind-Value="_nome" Label="Nome" Variant="Variant.Outlined" FullWidth="true"
                          Placeholder="Ex: João Silva, Empresa XYZ..." />
        </MudItem>
        <MudItem xs="12" Class="d-flex align-center gap-2">
            @if (!string.IsNullOrEmpty(_erro))
            {
                <MudAlert Severity="Severity.Error" Class="mb-0 flex-grow-1">@_erro</MudAlert>
            }
            <MudButton Color="Color.Error" Variant="Variant.Filled" OnClick="Salvar" Disabled="_salvando">
                @if (_salvando) { <MudProgressCircular Size="Size.Small" Indeterminate="true" Class="mr-2" /> }
                @(_editandoId == null ? "Adicionar" : "Salvar")
            </MudButton>
            @if (_editandoId != null)
            {
                <MudButton OnClick="Cancelar">Cancelar</MudButton>
            }
        </MudItem>
    </MudGrid>
</MudPaper>

@if (_carregando)
{
    <MudProgressLinear Color="Color.Error" Indeterminate="true" />
}
else if (_devedores?.Count > 0)
{
    <MudTable Items="_devedores" Hover="true" Striped="true" Dense="true" Breakpoint="Breakpoint.Xs" HorizontalScrollbar="true">
        <HeaderContent>
            <MudTh>Nome</MudTh>
            <MudTh>Criado em</MudTh>
            <MudTh></MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd>@context.Nome</MudTd>
            <MudTd>@context.DataCriacao.ToLocalTime().ToString("dd/MM/yyyy HH:mm")</MudTd>
            <MudTd>
                <MudIconButton Icon="@Icons.Material.Filled.Edit" Color="Color.Primary" Size="Size.Small"
                               OnClick="@(() => IniciarEdicao(context))" Title="Editar" />
                @if (_excluindoId == context.Id)
                {
                    <MudProgressCircular Size="Size.Small" Indeterminate="true" />
                }
                else
                {
                    <MudIconButton Icon="@Icons.Material.Filled.Delete" Color="Color.Error" Size="Size.Small"
                                   OnClick="@(() => Excluir(context.Id))" Title="Excluir" />
                }
            </MudTd>
        </RowTemplate>
    </MudTable>
    @if (!string.IsNullOrEmpty(_erroExclusao))
    {
        <MudAlert Severity="Severity.Error" Class="mt-3">@_erroExclusao</MudAlert>
    }
}
else if (!_carregando)
{
    <MudAlert Severity="Severity.Info">Nenhum devedor cadastrado. Adicione o primeiro acima.</MudAlert>
}

@code {
    private List<DevedorDto>? _devedores;
    private bool _carregando = true;
    private bool _salvando;
    private string _erro = string.Empty;
    private string _erroExclusao = string.Empty;
    private Guid? _excluindoId;
    private Guid? _editandoId;
    private string _nome = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        await Carregar();
    }

    private async Task Carregar()
    {
        _carregando = true;
        _devedores = await ApiService.ListarDevedoresAsync() ?? [];
        _carregando = false;
    }

    private void IniciarEdicao(DevedorDto devedor)
    {
        _editandoId = devedor.Id;
        _nome = devedor.Nome;
        _erro = string.Empty;
    }

    private void Cancelar()
    {
        _editandoId = null;
        _nome = string.Empty;
        _erro = string.Empty;
    }

    private async Task Salvar()
    {
        _erro = string.Empty;
        if (string.IsNullOrWhiteSpace(_nome) || _nome.Trim().Length < 2)
        {
            _erro = "Nome deve ter pelo menos 2 caracteres.";
            return;
        }

        _salvando = true;
        bool sucesso;
        string? erro;

        if (_editandoId == null)
            (sucesso, erro) = await ApiService.CriarDevedorAsync(_nome.Trim());
        else
            (sucesso, erro) = await ApiService.AtualizarDevedorAsync(_editandoId.Value, _nome.Trim());

        _salvando = false;

        if (sucesso)
        {
            Cancelar();
            await Carregar();
        }
        else
        {
            _erro = erro ?? "Erro ao salvar devedor.";
        }
    }

    private async Task Excluir(Guid id)
    {
        _erroExclusao = string.Empty;
        _excluindoId = id;
        var (sucesso, erro) = await ApiService.ExcluirDevedorAsync(id);
        _excluindoId = null;
        if (sucesso)
            await Carregar();
        else
            _erroExclusao = erro ?? "Erro ao excluir devedor.";
    }
}
