-- Script de inicialização do banco de dados
-- Criado automaticamente via EF Core Migrations — este arquivo é apenas para referência

-- Extensão para UUID
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- As tabelas são criadas automaticamente pelo EF Core Migration
-- Execute: dotnet ef database update --project src/MinhasFinancas.Infrastructure --startup-project src/MinhasFinancas.API
